RSpec.describe Next::Art::Cursor::Pagination do
  it "has a version number" do
    expect(Next::Art::Cursor::Pagination::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
